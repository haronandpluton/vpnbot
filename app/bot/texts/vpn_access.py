def format_datetime(value) -> str:
    if value is None:
        return "не указано"

    return value.strftime("%d.%m.%Y %H:%M")


def format_vpn_access_text(
    *,
    device_limit: int | None,
    expires_at,
) -> str:
    expires_at_text = format_datetime(expires_at)

    return (
        "Твоя VPN-подписка активна.\n\n"
        f"Устройств: {device_limit or '—'}\n"
        f"Активна до: {expires_at_text}\n\n"
        "Для подключения используй Happ VPN.\n\n"
        "Нажми «Подключить VPN», открой страницу подключения, скопируй профиль "
        "и добавь его в Happ VPN через импорт из буфера."
    )


def format_vpn_config_text(config_uri: str) -> str:
    return (
        "Страница подключения VPN:\n\n"
        "Нажми кнопку ниже, чтобы открыть страницу подключения.\n\n"
        "На странице:\n"
        "1. Нажми «Скопировать профиль для Happ VPN».\n"
        "2. Открой Happ VPN.\n"
        "3. Нажми добавление профиля.\n"
        "4. Выбери импорт из буфера обмена / Clipboard.\n"
        "5. Подтверди добавление профиля и включи VPN.\n\n"
        "Резервная ссылка:\n"
        f"<code>{config_uri}</code>"
    )


def happ_android_instruction_text() -> str:
    return (
        "Подключение через Happ VPN на Android:\n\n"
        "1. Нажми «Подключить VPN» в этом боте.\n"
        "2. Открой страницу подключения.\n"
        "3. Нажми «Скопировать профиль для Happ VPN».\n"
        "4. Открой Happ VPN.\n"
        "5. Нажми добавление профиля.\n"
        "6. Выбери импорт из буфера обмена / Clipboard.\n"
        "7. Подтверди добавление профиля.\n"
        "8. Включи VPN-подключение."
    )


def happ_ios_instruction_text() -> str:
    return (
        "Подключение на iPhone:\n\n"
        "1. Нажми «Подключить VPN» в этом боте.\n"
        "2. Открой страницу подключения.\n"
        "3. Нажми «Скопировать профиль для Happ VPN».\n"
        "4. Открой Happ VPN или другой клиент с поддержкой VLESS.\n"
        "5. Добавь профиль через импорт из буфера обмена, если приложение это поддерживает.\n"
        "6. Включи VPN."
    )


def happ_fallback_text() -> str:
    return (
        "Если автоматическое открытие Happ VPN не сработало:\n\n"
        "Это нормально. Telegram и Android не всегда передают VPN-ссылки напрямую в приложение.\n\n"
        "Рабочий способ:\n"
        "1. Открой страницу подключения.\n"
        "2. Нажми «Скопировать профиль для Happ VPN».\n"
        "3. Открой Happ VPN вручную.\n"
        "4. Добавь профиль через импорт из буфера обмена / Clipboard.\n\n"
        "Режим Subscription / URL может работать нестабильно, поэтому основной способ — импорт готового профиля из буфера."
    )